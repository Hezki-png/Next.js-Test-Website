'use client'; /* makes sure that this is loaded on the client side rendered */

const Hello = () => {
    console.log('I am a client side component');
    return (
        <div>Hello, I am the goat</div>
    )
}
export default Hello
